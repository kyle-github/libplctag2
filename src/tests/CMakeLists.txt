# Tests are organized into three categories:
#   unit/        — unit tests (single-module, no network)
#   integration/ — tests against a real or simulated PLC
#   blackbox/    — tests against the public API only
#
# Each subdirectory that contains a CMakeLists.txt is added here.
# add_subdirectory(unit)
# add_subdirectory(integration)
# add_subdirectory(blackbox)
